# StatusGateFrontend
StatusGate is an open-source service status platform for teams that want transparent, self-hosted status communication.  It provides public status pages with uptime timelines and incident history, automated HTTP/JSON/XML health checks, a background monitoring worker, and an admin dashboard to manage projects, components, and incident updates.
